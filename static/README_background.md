Coloca aquí la imagen que quieres usar como fondo:

- Guarda la imagen (por ejemplo la que adjuntaste) como `background.jpg` dentro de la carpeta `static` del proyecto.
  Ruta: `static/background.jpg`

- Reinicia la aplicación Flask si ya está corriendo o recarga la página en el navegador.

Si quieres, puedo guardar la imagen adjunta en `static/background.jpg` por ti — confirma y la guardo.